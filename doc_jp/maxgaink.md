# maxgaink

関数グラフにおいて、最大 K 回の移動で得られる最大利得を求めます。

## 関数

```go
func MaxGainK(i, N, K int, p, c []int) int
```

## MaxGainK

位置 `i` から開始し、`p` (1-indexed の次の位置) で定義される辺に沿って最大 K 回移動できます。各ノード `v` を訪れると `c[v]` の利得が得られます。到達可能な最大総利得を返します。関数グラフ内のサイクルを検出し、それを利用して計算を高速化します。
