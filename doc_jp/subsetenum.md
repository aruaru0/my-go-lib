# SubsetEnum — セットビットの部分集合列挙

## 関数

```go
func Subsets(i int) []int
```

Subsets は `i` のすべての非ゼロ部分マスクを返します。たとえば、`Subsets(0b1110)` は `[0b1110, 0b1100, 0b1010, 0b1000, 0b0110, 0b0100, 0b0010]` を返します。

標準的な部分マスク列挙パターン `for j := i; j != 0; j = (j - 1) & i` を使用します。
