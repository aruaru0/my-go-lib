# BaseConv — Negative Base Conversion

## Functions

- `ToBaseN[T Integer](x T, n T) []T` — converts integer x to base n, supporting negative bases (-2, -3, etc.). Returns digits in little-endian order (least significant first).
