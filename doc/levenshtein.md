# LevenshteinDistance — Edit Distance

## Functions

- `LevenshteinDistance(s, t string) int` — returns the Levenshtein distance (minimum number of single-character edits) between two strings

## Example

```go
d := mylib.LevenshteinDistance("kitten", "sitting")
// d = 3
```
