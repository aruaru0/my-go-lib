# Clique — Minimum Clique Cover

## Functions

```go
func MinCliqueCover(N int, edges [][]int) int
```

MinCliqueCover returns the minimum number of cliques needed to cover all vertices of an undirected graph. Uses bitmask DP.

A clique is a subset of vertices where every pair is directly connected by an edge.
